import Foundation

enum StringConstants {
    enum CellIdentifiers {
        static let matchCell = "MatchCell"
        static let playerCell = "PlayerCell"
    }

    enum Titles {
        static let liveMatches = "Live Matches"
        static let tournaments = "Tournaments"
        static let live = "Live"
        static let error = "Error"
        static let matchDetails = "Match Details"
    }

    enum Alert {
        static let errorTitle = "Error"
        static let okButton = "OK"
    }

    enum Buttons {
        static let ok = "OK"
        static let add = "Add"
        static let remove = "Remove"
    }

    enum Status {
        static let live = "LIVE"
        static let defaultScore = "-"
    }

    enum TabBar {
        static let live = "Live"
        static let tournaments = "Tournaments"
        static let rankings = "Rankings"
        static let favorites = "Favorites"
    }
}
